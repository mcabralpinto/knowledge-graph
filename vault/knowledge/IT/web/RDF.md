*Means Resource Description Framework. Defines a standard model for sharing information on the internet, based on triples.*

- Triplets have the form `(subject, predicate, object)`, with each of its parts being uniquely identified by a [[URI]].
- Expanded by [[OWL]] and queried using [[SPARQL]].